<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="login.css">
    <title>Login Page</title>
    <link rel="shortcut icon" href="images/favicon.svg" type="image/svg+xml">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
      *{
        text-decoration: none;
      }
      body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
      }
      #myVideo {
        position: fixed;
        right: 0;
        bottom: 0;
        min-width: 100%;
        min-height: 100%;
      }
      .container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }
      .registration {
        border: black
         solid 1PX;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    padding: 10px;
    border-radius: 5%;
    background-color: transparent;
    backdrop-filter: blur(0px);
   

        width: 300px;
        height: 300px;
      }
      h1 {
        font-size: 22px;
        font-weight: 600;
        color: #000000;
        font-family: 'Poppins', sans-serif;
      }
      .input-field {
        width: 100%;
        height: 40px;
        margin-bottom: 20px;
        position: relative;
      }
      .input-field input {
        width: 85%;
        height: 100%;
        padding: 0 15px;
        border: 1px solid #fff6f6;
        border-radius: 8px;
        outline: none;
        border: none;
      }
      .input-field .fa-regular {
        position: absolute;
        right: 13px;
        top: 50%;
        transform: translateY(-50%);
        font-size: 18px;
        color: #919191;
        cursor: pointer;
        padding: 3px;
      }
      .button {
        display: flex;
        justify-content: center;
        font-size: 1.3rem;
        text-transform: capitalize;
        letter-spacing: 1.5px;
        padding: 6px 1.2px;
      }
      .button input {
        color: #fff;
        font-size: 16px;
        font-weight: 400;
        background-color: #093b55;
        cursor: pointer;
        transition: all 0.3s ease;
      }
      .button input:hover {
        background-color: #a1d8f5;
      }
      .button input:active {
        transform: scale(0.95);
      }
      .error-text {
        font-size: 12px;
      }
      .login{
        color: black;
      }
    </style>
</head>
<body>
  <video autoplay muted loop id="myVideo">
    <source src="/stylesheet/bgvdo.mp4" type="video/mp4">
  </video>
  <div class="container">
    <div class="registration">
      <form action="/login" method="POST">
        <h1>Log In</h1>
        <div class="input-field">
          <input type="email" placeholder="Email" name="email" />
        </div>
        <div class="input-field">
          <input type="password" placeholder="Password" name="password" />
        </div>
        <div class="input-field button">
          <input type="submit" value="LOGIN" />
        </div>
        <div class="login_signup">Don't have an account? <a href="http://localhost:9000" class="login">Register</a></div>
      </form>
    </div>
  </div>
</body>
</html>
